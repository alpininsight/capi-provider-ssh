name: Private Reusable Workflow Smoke

on:
  pull_request:
    branches: [develop]

permissions:
  contents: read
  pull-requests: read

jobs:
  private-reusable-smoke:
    uses: alpininsight/.github-private/.github/workflows/repository-policy-reusable.yml@main
